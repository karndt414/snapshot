# URL of your deployed Vercel server
SNAPSHOT_SERVER_URL=https://your-app.vercel.app

# Must match API_SECRET_KEY in the Vercel environment variables
SNAPSHOT_API_KEY=your-secret-api-key-here

# Unique ID for this machine (defaults to hostname if not set)
MACHINE_ID=my-macbook-pro

# Display name shown in the dashboard
MACHINE_NAME=Koree's MacBook Pro
